{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with MSVC",
            "type": "cppvsdbg", // MSVC 调试器
            "request": "launch",
            "program": "${workspaceFolder}/build/Debug/OpenGL_Play.exe", // 可执行文件路径
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "integratedTerminal",
            "preLaunchTask": "CMake Build" // 调试前执行的任务
        }
    ]
}